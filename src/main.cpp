#include <iostream>
#include <vector>
#include <algorithm>
#include <fstream>
#include <string>
#include <chrono>
#include <thread>
#include <ctime>
#include <cmath>
#include <clocale>

using namespace std;

void BaslangicEkrani () {
  setlocale(LC_ALL, "Turkish");
  cout << "===========================================" << endl;
  cout << "        OLİMPİYAT OLASILIK ASİSTANI        " << endl;
  cout << "                 AYG LABS                  " << endl;
  cout << "===========================================" << endl;
    this_thread::sleep_for(chrono::seconds(1));
}
int main () {
  
